name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ gist:826940fc55aef4d195f3b9ee99da5ad1 }}
          GIST_ID: ${{ https://gist.github.com/ssyy5460/ }}
          TIMEZONE: Asia/Seoul
